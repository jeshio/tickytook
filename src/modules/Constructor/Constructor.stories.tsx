import { storiesOf } from '@storybook/react';
import * as React from 'react';
import ParamsReceiver from './ParamsReceiver';

const stories = storiesOf('Constructor', module);

stories.add('ParamsReceiver', () => <ParamsReceiver />);
